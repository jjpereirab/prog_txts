extremadamente minimal
	FROM alpine:latest 
	WORKDIR /workspace
	CMD ["bash"]          # Mantener el contenedor corriendo

con bash
	RUN apk update && apk add --no-cache bash # despues de FROM
	
con python 
	RUN apk update && apk add --no-cache python3 py3-pip
	
